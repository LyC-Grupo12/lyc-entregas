DIM [contador,promedio,actual,suma,cont] AS [Integer,Float,Float,Float,Float]
DIM [pivot,actual_reg] AS [Integer,Integer]


PUT "Prueba.txt LyC Tema 3!";
PUT "Ingrese un valor entero: ";
GET actual;

CONST nombre=85.0;
CONST constFloat=1.2;
CONST constString="prueba const string";

contador: 0;
*-Prueba *-doble-* comentario-*	
	*-Ejemplo de asignaciones-*

suma: 0.25+nombre;

while (contador <= 9) {
    contador: contador + 1;
    actual: cont/342.0 + (cont* maximo (actual*cont, 2.0, actual,maximo(4.0,actual,nombre) ) );
    suma: suma + actual;
}

PUT "La suma es: ";
PUT suma;

if (actual_reg > 2 and actual_reg <> 0)
    PUT "actual es mayor que 2 ";
else
    if (actual_reg < pivot)
        PUT "no es mayor que 2";
    endif
endif